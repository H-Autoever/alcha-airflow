# Airflow Docker Compose defaults
AIRFLOW_IMAGE_NAME=apache/airflow:2.10.2
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# Admin user (for local dev only)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Example connection (replace with real creds or set via UI)
# Format: mysql+mysqlconnector://user:password@host:port/db
AIRFLOW_CONN_MYSQL_ANALYTICS=mysql+mysqlconnector://root:password@127.0.0.1:3306/alcha_db

