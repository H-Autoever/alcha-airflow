# Airflow Docker Compose defaults
AIRFLOW_IMAGE_NAME=apache/airflow:2.10.2
AIRFLOW_UID=50000
AIRFLOW_PROJ_DIR=.

# Admin user (for local dev only)
_AIRFLOW_WWW_USER_USERNAME=airflow
_AIRFLOW_WWW_USER_PASSWORD=airflow

# Example: manage connections in Airflow UI for tests
# Leave connection env vars unset to avoid overriding UI values.
